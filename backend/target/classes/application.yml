spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/fixmatedb
    username: fixmate
    password: fixmate

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

server:
  port: 8080
